name: lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: true

      - name: Go mod download
        run: go mod download

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7   # ← v7 に更新
        with:
          version: v2.5.0                        # v2 系OK
          install-mode: binary                   # これが一番安定